# edu.cit.paradela.checkmateacademy
spring.application.name=checkmateacademy

# Supabase/PostgreSQL connection.
# Local defaults keep development working; set these environment variables in deployment.
spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:postgresql://aws-1-ap-southeast-2.pooler.supabase.com:6543/postgres?sslmode=require&prepareThreshold=0}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME:postgres.ukhqskgvqczvpqhfgxfz}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD:admin_123!321}
spring.datasource.driver-class-name=org.postgresql.Driver

# Comma-separated frontend origins allowed by CORS.
app.cors.allowed-origins=${APP_CORS_ALLOWED_ORIGINS:http://localhost:3000,http://localhost:3001,http://localhost:3002}

# Hibernate (JPA) Settings
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
